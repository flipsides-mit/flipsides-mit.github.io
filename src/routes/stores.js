import { writable } from 'svelte/store';

export const seldatap = writable([]);
export const seldataq = writable([]);
export const invselp = writable([true, true]);
export const invselq = writable([true, true]);
